// STUB — placeholder for packet-0013 coder. Not implemented yet (TDD red phase).
import type { ScoreResult } from "@/lib/types";
import type { CompanyNames } from "@/lib/shareImage";

export type SaveImageButtonProps = {
  result: ScoreResult;
  companyNames: CompanyNames;
};

export function SaveImageButton(_props: SaveImageButtonProps) {
  return null;
}
